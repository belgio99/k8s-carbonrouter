{{- if .Values.grafana.dashboards.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "carbonrouter-umbrella.fullname" . }}-trafficschedule-dashboard
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "carbonrouter-umbrella.labels" . | nindent 4 }}
    grafana_dashboard: "1"
data:
  trafficschedule-dashboard.json: |-
{{ .Files.Get "dashboards/trafficschedule-dashboard.json" | indent 4 }}
{{- end }}
